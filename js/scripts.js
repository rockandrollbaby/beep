var beepHorn = function(inputNumber) {
  if () {
    return true;
  } else: false;
}

$(function() {
  $("form#beep-horn").submit(e) {
    e.preventDefault();
    var result = beepHorn(inputNumber)

    $(".year").text(year);

  if () {

  } else {

  }
$("#result").show();

}

$(this).html(function(index, text) {
  return text.replace('0', "");
}

$("form#beep-horn").each(function)
